-- local msg = require "mp.msg"
-- local utils = require "mp.utils"
-- local options = require "mp.options"

-- function protec(str)
--     return (str)
-- end

-- -- local st = 'Hello $name!'
-- -- local newstring = st:gsub("$name", protec(protec('jop roskam')))
-- -- print(protec('hello world!'))
-- -- print(newstring)

-- function getname()
--     local filename = mp.get_property("path")
--     print(filename)
--     msg.info("FILENAME: " .. filename)
-- end


-- mp.add_key_binding("F", "getname", getname)

-- print(get_homedir())
